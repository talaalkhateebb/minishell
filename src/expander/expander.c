#include "minishell.h"

/*
** Expander stub — will be replaced in the next commit by the real
** module (expander.c + expander_var.c + expander_quotes.c).
*/
void	expand_cmds(t_cmd *cmds, t_shell *sh)
{
	(void)cmds;
	(void)sh;
}
